node_modules/
dist/
.expo/
web-build/
*.log
.DS_Store
.vercel/

# Native projects are generated by `expo prebuild` — do not commit.
/android/
/ios/

# Signing keys and secrets must never be committed.
*.keystore
!android/app/debug.keystore
*.jks
*.p12
*.p8
*.mobileprovision

# Release signing keys live here (see credentials/README.md).
/credentials/*
!/credentials/README.md
